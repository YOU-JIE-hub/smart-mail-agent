name: Lint
on:
  push: { branches: [ "**" ] }
  pull_request: { branches: [ "**" ] }
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install Ruff
        run: |
          python -m pip install -U pip
          pip install ruff==0.12.9
      - name: Ruff check (core only)
        run: |
          ruff check src modules init_db.py
      - name: Ruff format (dry-run, core only)
        run: |
          ruff format --check src modules init_db.py
