[pytest]
pythonpath = .
testpaths = tests/unit tests/contracts
markers =
    online: tests that require network or heavyweight environment bootstrap
    mailer_online: requires external mail services
addopts = --timeout=60 --timeout-method=thread -q
